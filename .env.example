# RubixLarq — Environment Variables
# Phase 1: No values required — everything runs client-side

# Phase 2+ (future use only — leave blank for now):
# VITE_SITE_URL=https://rubixlarq.com
# VITE_ANALYTICS_ID=
